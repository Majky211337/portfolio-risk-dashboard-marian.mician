[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    network: downloads live data from Yahoo Finance (skipped automatically when offline)
    app: drives the Streamlit app through streamlit.testing.AppTest
filterwarnings =
    ignore:.*missing ScriptRunContext.*
